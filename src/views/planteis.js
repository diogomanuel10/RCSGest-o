// Vista: Plantéis. Construída na Fase 7.
import { emptyHTML } from '../ui.js';

export function renderPlanteis(container) {
  container.innerHTML = `
    <header class="page-head"><h1 class="section-title">Plantéis</h1></header>
    ${emptyHTML('Em construção.')}
  `;
}
