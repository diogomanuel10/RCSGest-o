// Vista: Treinadores. Construída na Fase 9.
import { emptyHTML } from '../ui.js';

export function renderTreinadores(container) {
  container.innerHTML = `
    <header class="page-head"><h1 class="section-title">Treinadores</h1></header>
    ${emptyHTML('Em construção.')}
  `;
}
