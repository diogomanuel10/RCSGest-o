// Vista: Patrocínios. Construída na Fase 6.
import { emptyHTML } from '../ui.js';

export function renderPatrocinios(container) {
  container.innerHTML = `
    <header class="page-head"><h1 class="section-title">Patrocínios</h1></header>
    ${emptyHTML('Em construção.')}
  `;
}
