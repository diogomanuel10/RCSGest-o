// Vista: Calendário. Construída na Fase 8.
import { emptyHTML } from '../ui.js';

export function renderCalendario(container) {
  container.innerHTML = `
    <header class="page-head"><h1 class="section-title">Calendário</h1></header>
    ${emptyHTML('Em construção.')}
  `;
}
