// Vista: Definições (época, meta, backup). Construída na Fase 10.
import { emptyHTML } from '../ui.js';

export function renderDefinicoes(container) {
  container.innerHTML = `
    <header class="page-head"><h1 class="section-title">Definições</h1></header>
    ${emptyHTML('Em construção.')}
  `;
}
