// Vista: Painel (resumo). Construída na Fase 5.
import { emptyHTML } from '../ui.js';

export function renderPainel(container) {
  container.innerHTML = `
    <header class="page-head"><h1 class="section-title">Painel</h1></header>
    ${emptyHTML('Em construção.')}
  `;
}
